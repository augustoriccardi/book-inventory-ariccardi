datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Author {
  id    Int    @id @default(autoincrement())
  name  String
  books Book[] // Relación con el modelo Book
}

model Book {
  id               String    @id @default(uuid())
  title            String
  bookId           String
  series           String?
  author           Author    @relation(fields: [authorId], references: [id])
  authorId         Int
  rating           Float?
  description      String?
  language         String?
  isbn             String
  genres           String?
  characters       String?
  bookFormat       String?
  edition          String?
  pages            Int?
  publisher        String?
  publishDate      DateTime?
  firstPublishDate DateTime?
  awards           String?
  numRatings       Int?
  ratingsByStars   String?
  likedPercent     Float?
  setting          String?
  coverImg         String?
  bbeScore         Float?
  bbeVotes         Int?
  price            Float?
}
